{{render set-nav}}

<h3>{{name}} ({{cards.length}} cards)</h3>

<ul id="card-list">
  {{#each card in cards}}
    <li>
      <div class="card-front">
        {{card.front}}
      </div>
      <div class="card-back">
        {{card.back}}
      </div>
    </li>
  {{/each}}
</ul>
